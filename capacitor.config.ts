import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw-react-menu-ex',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
